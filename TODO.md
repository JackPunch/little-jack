# little-jack 改进清单

- [x] 增加配置验证，提前暴露缺失的必填项
- [ ] 手写 `.env` 解析器支持引号值
- [ ] 用构造函数替代 `Chat` 中的 nil 检查
- [ ] API 非 200 响应时返回详细错误信息
- [ ] 定义消息角色的常量，替代裸字符串
- [ ] `Thinking` 字段应按需发送，避免传 `disabled`
- [ ] `Stream` 配置与 `Chat` 实现不匹配
- [ ] 拆分文件，改善包结构

> 建议完成顺序：1 → 5 → 3 → 4 → 6 → 2 → 7 → 8
